server:
  port:8081

  spring:
    cloud:
      config:
        server:
          git:
            uri: https://github.com/developermx2020/Spring-Boot-JPA-.git
            searchPath: config-data
            username: ${GIT_USER}
            password: ${GIT_PASSWORD}
    security:
      user: root
      password: s3cr3t